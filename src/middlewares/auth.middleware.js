// Bridge re-export — source of truth moved to src/modules/auth/auth.middleware.js
// Kept for backward compatibility during phased migration.
module.exports = require('../modules/auth/auth.middleware');
